<%- include('./partials/head', {title: 'Catálogo'}) %>


<%- include('./partials/headerNavbar') %>

    <main>

      <div class="banner">

<%- include('./partials/slider')%>


      </div>

 
      <p>Todos los Productos</p>

      <section>

        <% productos.forEach((prod) => { %>
        <article>
          <form action="/products/carrito" method="post">
          <a href="/products/<%=prod.id%>" name="prodID">
            <img src="/images/<%=prod.image%>" alt="mundo" name="prodImage">
            <div class="articulo">
              <div class="autor-favorito">
                <h3><%=prod.title%></h3>
                <i class="fas fa-heart"></i>
              </div>
              <div class="precio-descuento">
                <p class="precio" name="prodPrice">$<%=toThousand(prod.price)%>
                  <a href="/products/<%=prod.id%>"><i class="fas fa-info-circle info"></i></a>
                </p>
                <!-- <a href="#agregar" class="comprar">Agregar</a> -->
                <button type="submit" class="comprar">Comprar</button>
              </div>
            </div>
          </a>
         </form>
          </article>
        <% }) %>
      </section>
<%- include('./partials/footer') %>